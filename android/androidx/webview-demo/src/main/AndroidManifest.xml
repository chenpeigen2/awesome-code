<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WebviewDemo"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 基础通信 -->
        <activity android:name=".basics.AndroidCallJsActivity" android:exported="false" />
        <activity android:name=".basics.JsCallAndroidActivity" android:exported="false" />

        <!-- 中级通信 -->
        <activity android:name=".intermediate.UrlSchemeActivity" android:exported="false" />
        <activity android:name=".intermediate.JsPromptActivity" android:exported="false" />

        <!-- 高级通信 -->
        <activity android:name=".advanced.BidirectionalActivity" android:exported="false" />
        <activity android:name=".advanced.WebMessagePortActivity" android:exported="false" />

    </application>

</manifest>
