<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- 存储权限 (用于文件下载示例) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- 前台服务权限 (用于长时间运行的任务) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- 后台执行限制豁免 (可选) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkManagerDemo"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 基础示例 Activity -->
        <activity android:name=".basic.BasicWorkerActivity" android:exported="false" />
        <activity android:name=".basic.OneTimeWorkActivity" android:exported="false" />
        <activity android:name=".basic.PeriodicWorkActivity" android:exported="false" />
        <activity android:name=".basic.WorkDataActivity" android:exported="false" />
        <activity android:name=".basic.WorkStatusActivity" android:exported="false" />
        
        <!-- 进阶示例 Activity -->
        <activity android:name=".advanced.WorkConstraintsActivity" android:exported="false" />
        <activity android:name=".advanced.WorkChainActivity" android:exported="false" />
        <activity android:name=".advanced.UniqueWorkActivity" android:exported="false" />
        <activity android:name=".advanced.WorkTagActivity" android:exported="false" />
        
        <!-- 高级示例 Activity -->
        <activity android:name=".expert.CoroutineWorkerActivity" android:exported="false" />
        <activity android:name=".expert.WorkExceptionActivity" android:exported="false" />
        
        <!-- 实战示例 Activity -->
        <activity android:name=".practice.DownloadFileActivity" android:exported="false" />
        <activity android:name=".practice.ImageCompressActivity" android:exported="false" />
        <activity android:name=".practice.DataSyncActivity" android:exported="false" />
        
        <!-- WorkManager 默认使用 JobScheduler，无需额外配置 -->
        <!-- 如需使用前台服务，可以在这里声明 -->
        
    </application>

</manifest>
