<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SecurityDemo" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="android:windowBackground">@color/surface</item>
    </style>

    <style name="Theme.SecurityDemo.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary</item>
        <item name="windowSplashScreenAnimatedIcon">@android:drawable/ic_menu_info_details</item>
        <item name="postSplashScreenTheme">@style/Theme.SecurityDemo</item>
    </style>
</resources>
