<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotificationDemo">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 通知Action接收器 -->
        <receiver
            android:name=".receiver.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.peter.notification.demo.ACTION_REPLY" />
                <action android:name="com.peter.notification.demo.ACTION_LIKE" />
                <action android:name="com.peter.notification.demo.ACTION_SHARE" />
                <action android:name="com.peter.notification.demo.ACTION_DELETE" />
                <action android:name="com.peter.notification.demo.ACTION_CONFIRM" />
                <action android:name="com.peter.notification.demo.ACTION_CANCEL" />
                <action android:name="com.peter.notification.demo.ACTION_DOWNLOAD" />
                <action android:name="com.peter.notification.demo.ACTION_PAUSE" />
                <action android:name="com.peter.notification.demo.ACTION_PLAY" />
                <action android:name="com.peter.notification.demo.ACTION_PREV" />
                <action android:name="com.peter.notification.demo.ACTION_NEXT" />
            </intent-filter>
        </receiver>

    </application>

</manifest>